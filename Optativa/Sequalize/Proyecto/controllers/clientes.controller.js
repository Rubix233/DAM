import { ClientesBaseController } from './base/clientes.base.controller.js';

class ClientesController extends ClientesBaseController {
    // Aquí puedes sobrescribir métodos del BaseController para personalizar la lógica
}

export default new ClientesController();
